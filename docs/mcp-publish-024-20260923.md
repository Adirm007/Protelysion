# 普罗泰利西翁 0.24.0 发布核验记录

日期：2026-09-23
发行标记：`protelysion-0.24.0-20260923`

## 发布范围

- 目标仓库：<https://github.com/Adirm007/Protelysion>，公网游戏：<https://adirm007.github.io/Protelysion/>。
- 本次只从 `22-发布/booksea-github/` 发行白名单发布；不把上级开发工作区、联调环境、Godot/Blender源工程、聊天数据或素材下载包带入发行仓库。
- 发布前核对的 `main` 基线为 `899cfcb3ec2549c4fbee36ecd142cf3523a22225`（0.23.0）。
- 本版本公开改动包括首页与游戏包更新、读者核心交接区更新、发行清单、0.24.0功能审计，以及本核验记录；未改公开站点以外的本机运行状态。

## 发布前验证

- 在发行目录执行 `python tools/materialize.py` 成功：逐项核对发行清单中的文件长度与 SHA-256，并解压校验 PCK/WASM 总长度及 SHA-256。
- 清单标记为 `protelysion-0.24.0-20260923`；目标范围为48主题、433种怪物、3031份生命层级模板。
- `mcp-seven-fixes-024-20260923.md` 记录本版本的完整验收证据：1103/1103回归、10/10功能浏览器检查、45/45发行浏览器检查、433/433立绘显示、缺图0项。手机视口模拟不等于真机长期性能验收。

## 公网部署

- `.github/workflows/pages.yml` 在 `main` 推送后运行 `python tools/materialize.py`，随后上传 `site/` 并部署 GitHub Pages。
- 发布完成应以 Actions 工作流成功，以及公网 `release-manifest.json` 的版本标记为准；本机发行目录或一次 Git 推送本身不代表 Pages 已完成部署。
- 安装页：<https://adirm007.github.io/Protelysion/loader.html>。

## 保留边界

- 不清空聊天、角色或远征存档；不调用真实模型；不上传密钥或非发行工作区内容。
- 仅发布经清单校验的白名单文件。第三方素材许可继续以 `LICENSE`、`NOTICE.md` 与各自许可文件为准。
